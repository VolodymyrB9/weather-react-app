import {Header, WeatherForm} from "../components";

const WeatherPage = () => {
    return (
        <div>
            <Header />
            <WeatherForm />
        </div>
    );
};

export {WeatherPage};